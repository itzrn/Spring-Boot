spring.application.name=hostpitalManagement

spring.datasource.url=jdbc:postgresql://localhost:5432/hospitalManagement
spring.datasource.username=postgres
spring.datasource.password=10702

# spring.jpa.hibernate.ddl-auto=
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true
# server.servlet.context-path=/api




# this is used to initialize the dummy data use in the table
spring.jpa.defer-datasource-initialization=true

# means always sql will run
spring.sql.init.mode=always

# location of data
spring.sql.init.data-locations=classpath:data.sql




